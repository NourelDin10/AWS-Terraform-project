# Terraform state files (VERY important)
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Terraform variables (contains passwords, keys, secrets)
*.tfvars
*.tfvars.json
terraform.tfvars
terraform.tfvars.json

# Local .terraform directory
.terraform/
.terraform.lock.hcl

# Sensitive files (SSH keys)
*.pem
*.key
*.pub

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# IDE / OS files
.DS_Store
Thumbs.db

